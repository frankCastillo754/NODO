export const metadata = {
  title: 'Registro NODO - Plataforma Corporativa',
  description: 'Comienza con NODO. Únete a la plataforma de trazabilidad verificada del futuro.',
}

export default function RegisterCrearLayout({
  children,
}: { children: React.ReactNode }) {
  return <>{children}</>
}
