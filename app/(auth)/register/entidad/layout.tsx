export const metadata = {
  title: 'Registro Cuenta Entidad | NODO',
  description: 'Registra tu entidad dentro del ecosistema NODO.',
}

export default function RegisterEntidadLayout({
  children,
}: { children: React.ReactNode }) {
  return <>{children}</>
}
